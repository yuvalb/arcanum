# Node Typescript CLI skeleton
A minimal typescript node CLI application leveraging `ts-node`, with `yargs` argument parser.

## Usage
### Local
```
npm i
npm start -- -b "hello world!"
```

### CLI
```
npm i - g
node-ts-cli -b "hello world!"
```